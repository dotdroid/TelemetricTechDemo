<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.dotdroid.telemetrictechdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TelemetricTechDemo">
        <activity
            android:name=".ui.DeviceMessagesActivity"/>
        <activity
            android:name=".ui.CameraScreenActivity"
            android:parentActivityName=".ui.DeviceCreateActivity"/>
        <activity
            android:name=".ui.DeviceCreateActivity"
            android:parentActivityName=".ui.DeviceListActivity"/>
        <activity android:name=".ui.DeviceListActivity"/>
        <activity
            android:name=".ui.DevicePagerActivity"
            android:parentActivityName=".ui.DeviceListActivity">
        </activity>
        <activity
            android:name=".ui.LoginScreenActivity"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>